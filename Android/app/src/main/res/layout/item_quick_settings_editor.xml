<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.AVORA.Settings.NavigationCard"
    android:layout_width="match_parent"
    android:layout_height="54dp"
    android:layout_marginBottom="7dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <CheckBox
            android:id="@+id/checkQuickSetting"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:buttonTint="@color/primary"
            android:gravity="center" />

        <TextView
            android:id="@+id/txtQuickSettingEditorTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textAppearance="@style/TextAppearance.AVORA.Settings.RowTitle" />

        <ImageButton
            android:id="@+id/btnQuickSettingUp"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/settings_quick_move_up"
            android:padding="10dp"
            android:src="@drawable/ic_arrow_up_20"
            app:tint="@color/primary" />

        <ImageButton
            android:id="@+id/btnQuickSettingDown"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/settings_quick_move_down"
            android:padding="10dp"
            android:src="@drawable/ic_arrow_down_20"
            app:tint="@color/primary" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>