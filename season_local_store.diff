diff --git a/Android/app/src/main/java/com/ali/smartgarden/journal/LocalSeasonOutcomeStore.java b/Android/app/src/main/java/com/ali/smartgarden/journal/LocalSeasonOutcomeStore.java
--- a/Android/app/src/main/java/com/ali/smartgarden/journal/LocalSeasonOutcomeStore.java
+++ b/Android/app/src/main/java/com/ali/smartgarden/journal/LocalSeasonOutcomeStore.java
@@ -37,6 +37,7 @@ public final class LocalSeasonOutcomeStore {
             JSONObject item = new JSONObject();
             item.put("id", value.getId()); item.put("zone_id", value.getZone_id());
+            item.put("season_id", value.getSeason_id());
             item.put("result", value.getResult()); item.put("harvest_amount", value.getHarvest_amount());
@@ -50,6 +51,32 @@ public final class LocalSeasonOutcomeStore {
         return value;
     }
+
+    /** Persists the exact season outcome produced by the atomic close operation. */
+    public SeasonOutcome addForSeason(SeasonOutcome value) {
+        if (value == null) throw new IllegalArgumentException("Sezon sonucu gerekli");
+        if (value.getId().isBlank()) value.setId(UUID.randomUUID().toString());
+        if (value.getRecorded_at_epoch() <= 0L) value.setRecorded_at_epoch(System.currentTimeMillis() / 1000L);
+        JSONArray items = read();
+        for (int i = 0; i < items.length(); i++) {
+            JSONObject current = items.optJSONObject(i);
+            if (current != null && value.getId().equals(current.optString("id"))) return value;
+        }
+        try {
+            JSONObject item = new JSONObject();
+            item.put("id", value.getId()); item.put("zone_id", value.getZone_id());
+            item.put("season_id", value.getSeason_id()); item.put("result", value.getResult());
+            item.put("harvest_amount", value.getHarvest_amount());
+            item.put("next_season_note", value.getNext_season_note());
+            item.put("recorded_at_epoch", value.getRecorded_at_epoch());
+            item.put("yield_note", value.getYield_note()); item.put("issues_note", value.getIssues_note());
+            item.put("successful_practices", value.getSuccessful_practices());
+            item.put("water_summary", value.getWater_summary()); item.put("fertilizer_summary", value.getFertilizer_summary());
+            items.put(item);
+            context.getSharedPreferences(PREFS, Context.MODE_PRIVATE).edit().putString(KEY_ITEMS, items.toString()).apply();
+        } catch (Exception error) { throw new IllegalStateException("Sezon sonucu kaydedilemedi", error); }
+        return value;
+    }
 
     public List<SeasonOutcome> load() {
@@ -57,6 +84,7 @@ public final class LocalSeasonOutcomeStore {
         for (int i = 0; i < items.length(); i++) try {
             JSONObject item = items.getJSONObject(i); SeasonOutcome value = new SeasonOutcome();
             value.setId(item.optString("id")); value.setZone_id(item.optString("zone_id")); value.setResult(item.optString("result"));
+            value.setSeason_id(item.optString("season_id"));
             value.setHarvest_amount(item.optString("harvest_amount")); value.setNext_season_note(item.optString("next_season_note"));
diff --git a/Android/app/src/main/java/com/ali/smartgarden/journal/LocalGardenEventStore.java b/Android/app/src/main/java/com/ali/smartgarden/journal/LocalGardenEventStore.java
--- a/Android/app/src/main/java/com/ali/smartgarden/journal/LocalGardenEventStore.java
+++ b/Android/app/src/main/java/com/ali/smartgarden/journal/LocalGardenEventStore.java
@@ -27,6 +27,12 @@ public final class LocalGardenEventStore {
     public GardenEvent add(String zoneId, String type, String note) {
         return addInternal(zoneId, type, note, "MANUAL", "");
     }
+    public GardenEvent addForSeason(String zoneId, String seasonId, String type, String note) {
+        GardenEvent event = addInternal(zoneId, type, note, "MANUAL", "");
+        event.setSeason_id(seasonId);
+        replaceSeasonId(event.getId(), seasonId);
+        return event;
+    }
     public GardenEvent add(String zoneId, String type, String note, long occurredAtEpoch) {
@@ -60,6 +66,7 @@ public final class LocalGardenEventStore {
             item.put("id", event.getId());
             item.put("zone_id", event.getZone_id());
+            item.put("season_id", event.getSeason_id());
             item.put("type", event.getType());
@@ -87,6 +94,17 @@ public final class LocalGardenEventStore {
                 .putString(KEY_INDEX, index.toString()).apply();
     }
+    private void replaceSeasonId(String id, String seasonId) {
+        JSONArray index = read();
+        for (int i = 0; i < index.length(); i++) {
+            JSONObject item = index.optJSONObject(i);
+            if (item == null || !id.equals(item.optString("id"))) continue;
+            try { item.put("season_id", seasonId == null ? "" : seasonId); } catch (Exception ignored) { }
+        }
+        context.getSharedPreferences(PREFS, Context.MODE_PRIVATE).edit()
+                .putString(KEY_INDEX, index.toString()).apply();
+    }
     public List<GardenEvent> load() {
@@ -99,6 +117,7 @@ public final class LocalGardenEventStore {
                 event.setId(item.optString("id"));
                 event.setZone_id(item.optString("zone_id"));
+                event.setSeason_id(item.optString("season_id"));
                 event.setType(item.optString("type"));
